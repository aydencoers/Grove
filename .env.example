# Finnhub API key — https://finnhub.io/dashboard (free tier is enough).
# Read server-side only (lib/finnhub.ts); never exposed to the browser.
# Without it, the app falls back to each position's dated reference price.
FINNHUB_API_KEY=
